<div class="gray_background">
  <div class="wrapper clearfix">
    <div class="main_content">
      <h3>Donate $<%= Settings.price %></h3>
      <p>
        Enter your email address below.
      </p>

      <%= form_tag "/preorder/prefill", :id => "checkout" do %>
        <%= email_field_tag "email", @email, :placeholder => "me@squareup.com", :required => "required", :id => "email" %>
        <%= hidden_field_tag "preorder", true %>

        <br />
        <br />
        <%= render 'stripe_button' %>
      <% end %>
    </div>
    <%= render 'preorder/checkout/sidebar' %>
  </div>
</div>
